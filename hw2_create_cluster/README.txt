Очень долго не мог создать кластер и закачать все данные в s3

В итоге данные закачал скриптом, потомц что при загрузке всех файлов в папке отключался от VM почему-то ¯\_(ツ)_/¯

Кластер не мог создать потому что не добавлялись правила в группе безомастности (см скрины в папке)
Добавилось только через YC-cli (пришлось неделю с поддержкой пинаться)

```
yc vpc security-group update-rules otus-dataproc --add-rule "description=ANY,direction=egress,from-port=0,to-port=65535,protocol=any,v4-cidrs=0.0.0.0/0"
```

Ещё и сам кластер после создания не вышел из save-mod пришлось пере создавать